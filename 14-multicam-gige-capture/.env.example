# cp .env.example .env  후 현장 값으로 채운다. .env 는 커밋하지 않는다.

# 녹화 파일 저장 디렉터리 (Jetson 에서는 별도 SSD 마운트 경로 권장)
VIDEOS_DIR=/mnt/ssd/potato_videos

# 카메라 시리얼별 설치 방향 보정. "시리얼:회전여부" 를 쉼표로 나열 (1 = 180도 회전)
# pylon 의 장치 열거 순서는 보장되지 않으므로 인덱스가 아니라 시리얼로 묶는다.
CAMERA_ROTATE_180=25xxxxxx:0,25yyyyyy:1
